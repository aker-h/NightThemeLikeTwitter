//DOM構成中
(async () => {
    if (onTopPageOfImageSearch(location.href)) {
        addLogoOfImageSearch();
    }
    
    changeGoooooooooogle();

    return VOID_PROMISE;
})();

//DOM構成後
$(() => {
    return;
});